name: Development Build

on:
  push:
    branches-ignore:
      - Release

jobs:
  build:
    runs-on: home-dev

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Maven
        run: |
          sudo apt-get update -q
          sudo apt-get install -y -q maven

      - name: Configure Maven settings
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml << 'EOF'
          <settings>
            <servers>
              <server>
                <id>Snapshots</id>
                <username>deployment</username>
                <password>${{ secrets.NEXUS_PASSWORD }}</password>
              </server>
              <server>
                <id>Releases</id>
                <username>deployment</username>
                <password>${{ secrets.NEXUS_PASSWORD }}</password>
              </server>
              <server>
                <id>main</id>
                <username>deployment</username>
                <password>${{ secrets.NEXUS_PASSWORD }}</password>
              </server>
            </servers>
            <mirrors>
              <mirror>
                <id>main</id>
                <mirrorOf>central</mirrorOf>
                <url>http://nexus.jbrmmg.me.uk:8081/repository/maven-public/</url>
              </mirror>
            </mirrors>
            <profiles>
              <profile>
                <id>default</id>
                <properties>
                  <repository.url>http://nexus.jbrmmg.me.uk:8081/repository</repository.url>
                </properties>
              </profile>
            </profiles>
            <activeProfiles>
              <activeProfile>default</activeProfile>
            </activeProfiles>
          </settings>
          EOF

      - name: Build and Test with Maven
        run: mvn verify

      - name: Run Sonar
        run: mvn sonar:sonar -Dsonar.token=${{ secrets.SONAR_TOKEN }}

      - name: Build Docker image
        run: |
          docker build \
            -f src/main/resources/docker/Dockerfile \
            -t nexus.jbrmmg.me.uk:8083/home-dev:${{ github.sha }} \
            -t nexus.jbrmmg.me.uk:8083/home-dev:latest \
            .

      - name: Push to Nexus Docker registry
        run: |
          docker login nexus.jbrmmg.me.uk:8083 \
            -u deployment \
            -p ${{ secrets.NEXUS_PASSWORD }}
          docker push nexus.jbrmmg.me.uk:8083/home-dev:${{ github.sha }}
          docker push nexus.jbrmmg.me.uk:8083/home-dev:latest

      - name: Deploy
        env:
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          EMAIL_KEY: ${{ secrets.EMAIL_KEY }}
        run: |
          docker login nexus.jbrmmg.me.uk:8084 \
            -u deployment \
            -p ${{ secrets.NEXUS_PASSWORD }}
          echo "SMTP_PASSWORD=${SMTP_PASSWORD}" > .env
          echo "EMAIL_KEY=${EMAIL_KEY}" >> .env
          docker compose -f docker-compose-dev.yml pull
          docker compose -f docker-compose-dev.yml up -d
